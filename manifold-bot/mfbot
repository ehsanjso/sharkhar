#!/bin/bash
# Manifold Markets Paper Trading Bot CLI
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
cd "$SCRIPT_DIR"
[ -d "venv" ] && source venv/bin/activate
export PYTHONIOENCODING=utf-8
python3 bot.py "$@"
